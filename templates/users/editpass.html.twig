{% extends 'users.html.twig' %}

{% block title %}Modifier mot de passe {{ app.user.firstname }} {{ app.user.name }}{% endblock %}

{% block body %}
 <div class=" grix-xs4 container">
 {% for message in app.flashes('error') %}
  <div class="col-xs4">
       <div class="p-3 my-2 rounded-2 error">{{message}}</div>
       </div>
       {% endfor %}
      <div class="col-sx4">
        <h1>Modifier mot de passe de {{ app.user.firstname }} {{ app.user.name }} ({{ app.user.traduc|length }})</h1>
        <form method="post">
        <div class="form-field">
        <label for="pass">Mot de passe</label>
        <input type="password" id="pass" name="pass" class="form-control">
        </div>
        <div class="form-field">
        <label for="pass2">Confirmer le Mot de passe</label>
        <input type="password" id="pass2" name="pass2" class="form-control">
    </div>
    <button class="btn blue">Valider</button>
    </form>
</div>
{% endblock %}
