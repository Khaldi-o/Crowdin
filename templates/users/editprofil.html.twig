{% extends 'users.html.twig' %}

{% block title %}Modifiction du Profil de {{ app.user.firstname }} {{ app.user.name }}{% endblock %}

{% block body %}
 <div class=" grix-xs4 container">
      <div class="col-sx4">
        <h1>Modification du profil de {{ app.user.firstname }} {{ app.user.name }} ({{ app.user.traduc|length }})</h1>
        {{ form(form) }}
    </div>
</div>
{% endblock %}
